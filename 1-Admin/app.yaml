runtime: nodejs18

service: admin  # This defines the service name for the admin app

handlers:
- url: /(.*\..+)$
  static_files: build/\1
  upload: build/(.*\..+)$

- url: /.*
  static_files: build/index.html
  upload: build/index.html